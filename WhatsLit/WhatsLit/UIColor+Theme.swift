/*
See LICENSE folder for this sample’s licensing information.

Abstract:
Utility file that defines constants for colors declared in the asset catalog.
*/

import UIKit

extension UIColor {
    static let barColor = UIColor(named: "barColor")
    static let restaurantColor = UIColor(named: "restaurantColor")
    static let clubColor = UIColor(named: "clubColor")
    static let translucentButtonColor = UIColor(named: "translucentButton")
}
